/**
 * Fiches sportives / objectifs sportifs : la structure (composants, actions, données)
 * est conservée mais l'UI est masquée pour le moment. Repasser à `true` pour tout réafficher.
 */
export const SPORT_FEATURES_ENABLED = false;
